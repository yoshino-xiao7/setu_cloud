# 图片删除申请功能 - 前端开发文档

## 📋 功能需求概述

### 业务背景
用户在使用"积分调用"或"博客调用"获取图片后，如果觉得图片内容质量过低，可以提交申请删除该图片。管理员需要审查确认后才能在数据库中删除该图片及其相关内容。

### 功能模块
1. **用户端**：提交删除申请、查看我的申请列表和详情
2. **管理员端**：查看待审核列表、查看申请详情（含完整图片信息）、批准或拒绝申请

---

## 🔌 API 接口规范

### 通用说明

- **Base URL**: `/api` (根据实际部署调整)
- **认证方式**: Bearer Token (在 Header 中携带 `Authorization: Bearer <token>`)
- **Content-Type**: `application/json`

### 状态码说明

| 状态值 | 状态文本 | 说明 |
|--------|----------|------|
| `0` | 待审核 | 用户已提交，等待管理员处理 |
| `1` | 已批准 | 管理员批准，图片已从数据库删除 |
| `2` | 已拒绝 | 管理员拒绝，图片保留 |

---

## 📱 用户端 API

### 1. 提交删除申请

**请求**
```http
POST /image-delete/submit
Authorization: Bearer <user_token>
Content-Type: application/json
```

**请求体**
```json
{
  "pid": 12345678,
  "p": 0,
  "reason": "图片质量过低，画面模糊不清"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pid` | Integer | ✅ | 图片的 Pixiv 作品ID |
| `p` | Integer | ✅ | 图片在作品中的页码（从0开始） |
| `reason` | String | ❌ | 申请删除的原因（建议必填以便管理员审核） |

**成功响应**
```json
"提交成功，请等待管理员审核"
```

**错误响应**
```json
{
  "timestamp": "2026-01-15T11:00:00.000+00:00",
  "status": 500,
  "error": "Internal Server Error",
  "message": "图片不存在",
  "path": "/image-delete/submit"
}
```

可能的错误信息：
- `"pid 和 p 不能为空"`
- `"图片不存在"`
- `"您已对此图片提交过删除申请，请等待审核"`
- `"未登录或 Token 无效"`

---

### 2. 查看我的申请列表

**请求**
```http
GET /image-delete/my?page=1&pageSize=10
Authorization: Bearer <user_token>
```

**查询参数**
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `page` | Integer | 1 | 页码 |
| `pageSize` | Integer | 10 | 每页条数（最大50） |

**成功响应**
```json
{
  "total": 25,
  "page": 1,
  "pageSize": 10,
  "list": [
    {
      "id": 1,
      "userId": 123,
      "userEmail": "user@example.com",
      "userNickname": "用户昵称",
      "pid": 12345678,
      "p": 0,
      "reason": "图片质量过低",
      "status": 0,
      "statusText": "待审核",
      "createdAt": "2026-01-15T10:30:00",
      "imageTitle": "作品标题",
      "imageAuthor": "作者名",
      "thumbnailUrl": "https://i.pximg.net/img-original/..."
    }
  ]
}
```

**列表项字段说明**
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 申请ID |
| `userId` | Long | 申请人用户ID |
| `userEmail` | String | 申请人邮箱 |
| `userNickname` | String | 申请人昵称 |
| `pid` | Integer | 图片 pid |
| `p` | Integer | 图片页码 |
| `reason` | String | 申请原因 |
| `status` | Integer | 状态码 (0/1/2) |
| `statusText` | String | 状态文本 |
| `createdAt` | String | 申请时间 (ISO 8601) |
| `imageTitle` | String | 图片标题（可能为null，图片已删除时） |
| `imageAuthor` | String | 图片作者（可能为null） |
| `thumbnailUrl` | String | 缩略图URL（可能为null） |

---

### 3. 查看我的申请详情

**请求**
```http
GET /image-delete/my/{id}
Authorization: Bearer <user_token>
```

**路径参数**
| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 申请ID |

**成功响应**
```json
{
  "id": 1,
  "userId": 123,
  "userEmail": "user@example.com",
  "userNickname": "用户昵称",
  "reason": "图片质量过低，画面模糊",
  "status": 0,
  "statusText": "待审核",
  "createdAt": "2026-01-15T10:30:00",
  
  "pid": 12345678,
  "p": 0,
  "title": "作品标题",
  "author": "画师名",
  "uid": 87654321,
  "r18": 0,
  "width": 1920,
  "height": 1080,
  "ext": "jpg",
  "aiType": 0,
  "uploadDate": 1704067200000,
  "urlOriginal": "https://i.pximg.net/img-original/...",
  "tags": ["tag1", "tag2", "tag3"],
  
  "adminId": null,
  "adminEmail": null,
  "adminRemark": null,
  "reviewedAt": null
}
```

**错误响应**
- `"删除申请不存在"`
- `"无权查看此申请"` (尝试查看他人的申请)

---

## 🔐 管理员端 API

> ⚠️ 以下接口需要管理员权限 (role = 1)

### 1. 获取删除申请列表

**请求**
```http
GET /admin/image-delete/list?status=0&page=1&pageSize=20
Authorization: Bearer <admin_token>
```

**查询参数**
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `status` | Integer | (不传) | 按状态过滤：0=待审核, 1=已批准, 2=已拒绝，不传=全部 |
| `page` | Integer | 1 | 页码 |
| `pageSize` | Integer | 20 | 每页条数（最大50） |

**成功响应**
```json
{
  "total": 100,
  "page": 1,
  "pageSize": 20,
  "list": [
    {
      "id": 1,
      "userId": 123,
      "userEmail": "user@example.com",
      "userNickname": "用户昵称",
      "pid": 12345678,
      "p": 0,
      "reason": "图片质量过低",
      "status": 0,
      "statusText": "待审核",
      "createdAt": "2026-01-15T10:30:00",
      "imageTitle": "作品标题",
      "imageAuthor": "作者名",
      "thumbnailUrl": "https://i.pximg.net/img-original/..."
    }
  ]
}
```

---

### 2. 获取待审核列表（快捷接口）

**请求**
```http
GET /admin/image-delete/pending?page=1&pageSize=20
Authorization: Bearer <admin_token>
```

**说明**: 等同于 `/admin/image-delete/list?status=0`，专门获取待审核的申请。

---

### 3. 获取申请详情

**请求**
```http
GET /admin/image-delete/{id}
Authorization: Bearer <admin_token>
```

**成功响应**
```json
{
  "id": 1,
  "userId": 123,
  "userEmail": "user@example.com",
  "userNickname": "用户昵称",
  "reason": "图片质量过低，画面模糊不清",
  "status": 0,
  "statusText": "待审核",
  "createdAt": "2026-01-15T10:30:00",
  
  "pid": 12345678,
  "p": 0,
  "title": "夏日海边",
  "author": "画师A",
  "uid": 87654321,
  "r18": 0,
  "width": 1920,
  "height": 1080,
  "ext": "jpg",
  "aiType": 0,
  "uploadDate": 1704067200000,
  "urlOriginal": "https://i.pximg.net/img-original/img/2024/01/01/00/00/00/12345678_p0.jpg",
  "tags": ["女孩", "海边", "夏天", "泳装"],
  
  "adminId": null,
  "adminEmail": null,
  "adminRemark": null,
  "reviewedAt": null
}
```

**详情字段完整说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 申请ID |
| `userId` | Long | 申请人ID |
| `userEmail` | String | 申请人邮箱 |
| `userNickname` | String | 申请人昵称 |
| `reason` | String | 申请删除原因 |
| `status` | Integer | 状态码 |
| `statusText` | String | 状态文本 |
| `createdAt` | String | 申请时间 |
| `pid` | Integer | Pixiv 作品ID |
| `p` | Integer | 作品页码 |
| `title` | String | 作品标题 |
| `author` | String | 作者名 |
| `uid` | Integer | 作者 Pixiv UID |
| `r18` | Integer | R18标记 (0=否, 1=是) |
| `width` | Integer | 图片宽度 (px) |
| `height` | Integer | 图片高度 (px) |
| `ext` | String | 文件扩展名 |
| `aiType` | Integer | AI类型 (0=未知, 1=非AI, 2=AI生成) |
| `uploadDate` | Long | 上传时间戳(毫秒) |
| `urlOriginal` | String | 原图URL（可用于预览） |
| `tags` | String[] | 标签数组 |
| `adminId` | Long | 审核管理员ID（未审核时为null） |
| `adminEmail` | String | 审核管理员邮箱 |
| `adminRemark` | String | 管理员备注 |
| `reviewedAt` | String | 审核时间 |

---

### 4. 审核删除申请

**请求**
```http
POST /admin/image-delete/review
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**请求体**
```json
{
  "requestId": 1,
  "approve": true,
  "remark": "已确认图片质量问题，批准删除"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `requestId` | Long | ✅ | 要审核的申请ID |
| `approve` | Boolean | ✅ | `true`=批准删除, `false`=拒绝 |
| `remark` | String | ❌ | 管理员备注 |

**成功响应**

批准时：
```json
"已批准删除申请，图片已从数据库移除"
```

拒绝时：
```json
"已拒绝删除申请"
```

**错误响应**
- `"申请ID不能为空"`
- `"请指定批准或拒绝"`
- `"删除申请不存在"`
- `"该申请已被处理"`

---

## 🎨 UI 设计需求

### 用户端界面

#### 1. 提交删除申请入口

**触发位置**：
- 积分调用页面 - 图片查看区域的操作按钮
- 博客调用页面 - 图片展示卡片的操作菜单
- 图片详情弹窗 - 添加"举报/申请删除"按钮

**UI示意**：
```
┌─────────────────────────────────────┐
│  [图片预览区域]                      │
│                                     │
│  ┌─────────────────────────────────┐│
│  │ 📥 下载  ❤️ 收藏  🗑️ 申请删除   ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

#### 2. 申请删除弹窗/对话框

```
┌─────────────────────────────────────────┐
│  🗑️ 申请删除图片                    ✕  │
├─────────────────────────────────────────┤
│                                         │
│  您正在申请删除以下图片：               │
│  ┌─────────────────────────────────┐   │
│  │ [缩略图]  标题：夏日少女          │   │
│  │           作者：画师A             │   │
│  │           PID：12345678           │   │
│  └─────────────────────────────────┘   │
│                                         │
│  删除原因：                             │
│  ┌─────────────────────────────────┐   │
│  │ [文本输入框，多行]                │   │
│  │ 例如：图片质量过低、画面模糊...   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ⚠️ 提交后需等待管理员审核             │
│                                         │
│       [ 取消 ]    [ 提交申请 ]         │
└─────────────────────────────────────────┘
```

#### 3. 我的申请列表页

**入口位置**：用户中心/个人设置中添加"我的删除申请"菜单项

```
┌──────────────────────────────────────────────────────┐
│  我的删除申请                                         │
├──────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────┐  │
│  │ [缩略图]  夏日少女                              │  │
│  │           申请时间：2026-01-15 10:30           │  │
│  │           状态：🟡 待审核                       │  │
│  │           原因：图片质量过低...                 │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │ [缩略图]  夜景城市                              │  │
│  │           申请时间：2026-01-14 15:20           │  │
│  │           状态：🟢 已批准                       │  │
│  │           管理员备注：已确认并删除              │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │ [缩略图]  森林风景                              │  │
│  │           申请时间：2026-01-13 09:15           │  │
│  │           状态：🔴 已拒绝                       │  │
│  │           管理员备注：图片质量正常，不予删除    │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│              [1] [2] [3] ... [10]  分页              │
└──────────────────────────────────────────────────────┘
```

**状态标识颜色**：
- 🟡 黄色/橙色 - 待审核 (status=0)
- 🟢 绿色 - 已批准 (status=1)
- 🔴 红色 - 已拒绝 (status=2)

---

### 管理员端界面

#### 1. 删除申请管理页入口

在管理后台侧边栏添加菜单项：
```
📊 管理后台
├── 👥 用户管理
├── 🖼️ 图片删除申请  ← 新增
│   └── 显示待审核数量徽章
├── 🚫 IP黑名单管理
└── ...
```

#### 2. 申请列表页

```
┌────────────────────────────────────────────────────────────────┐
│  🖼️ 图片删除申请管理                     待审核: 12 条         │
├────────────────────────────────────────────────────────────────┤
│  状态筛选: [全部▼] [待审核] [已批准] [已拒绝]                   │
├────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ #1 | 🟡待审核                                             │  │
│  │ ┌─────────┐ 标题：夏日少女                                │  │
│  │ │[缩略图] │ PID：12345678  作者：画师A                    │  │
│  │ └─────────┘ 申请人：user@example.com (用户昵称)           │  │
│  │              申请时间：2026-01-15 10:30                   │  │
│  │              原因：图片质量过低，画面模糊                  │  │
│  │                                    [查看详情] [快速审核▼] │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ #2 | 🟢已批准                                             │  │
│  │ ... (已处理的申请折叠显示)                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│                    [1] [2] [3] ... [5]  分页                   │
└────────────────────────────────────────────────────────────────┘
```

#### 3. 申请详情页/弹窗

管理员点击"查看详情"后显示完整信息：

```
┌─────────────────────────────────────────────────────────────────┐
│  📋 删除申请详情 #1                                        ✕   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ═══ 申请信息 ═══                                              │
│  申请人：user@example.com (用户昵称)                           │
│  申请时间：2026-01-15 10:30:00                                 │
│  申请原因：图片质量过低，画面模糊，不适合展示                   │
│  当前状态：🟡 待审核                                            │
│                                                                 │
│  ═══ 图片信息 ═══                                              │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │              [大图预览区域]                                │ │
│  │              (点击可放大查看原图)                          │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  PID: 12345678    页码: 0                                      │
│  标题: 夏日少女                                                 │
│  作者: 画师A (UID: 87654321)                                   │
│  尺寸: 1920 × 1080                                             │
│  格式: JPG                                                      │
│  R18: 否       AI生成: 否                                      │
│  上传时间: 2024-01-01 00:00:00                                 │
│  标签: 女孩, 海边, 夏天, 泳装, 阳光                            │
│                                                                 │
│  ═══ 审核操作 ═══                                              │
│  备注 (可选)：                                                  │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ [输入审核备注...]                                         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ⚠️ 批准后将永久删除该图片及其所有关联数据                      │
│                                                                 │
│        [ 🔴 拒绝申请 ]              [ 🟢 批准删除 ]            │
└─────────────────────────────────────────────────────────────────┘
```

#### 4. 审核确认弹窗

**批准删除确认**：
```
┌─────────────────────────────────────┐
│  ⚠️ 确认批准删除？                   │
├─────────────────────────────────────┤
│                                     │
│  您即将批准删除以下图片：            │
│  PID: 12345678                      │
│  标题: 夏日少女                      │
│                                     │
│  ⚠️ 此操作将永久删除：               │
│  • 图片主记录                       │
│  • 所有尺寸的图片URL                │
│  • 图片关联的标签                   │
│                                     │
│  此操作不可撤销！                    │
│                                     │
│     [ 取消 ]    [ 确认删除 ]        │
└─────────────────────────────────────┘
```

---

## 💡 前端实现建议

### 技术栈建议
- 使用现有项目的 UI 框架和样式系统
- 复用现有的 HTTP 请求封装（如 axios 实例）
- 复用现有的用户认证机制

### 组件划分建议

```
components/
├── ImageDeleteRequest/
│   ├── SubmitDialog.vue          # 提交申请弹窗
│   ├── MyRequestList.vue         # 用户-我的申请列表
│   ├── RequestCard.vue           # 申请卡片组件
│   ├── StatusBadge.vue           # 状态徽章组件
│   └── admin/
│       ├── RequestListPage.vue   # 管理员-列表页
│       ├── RequestDetailModal.vue # 管理员-详情弹窗
│       └── ReviewActions.vue     # 审核操作按钮组
```

### 状态管理

```typescript
// 状态常量
const REQUEST_STATUS = {
  PENDING: 0,   // 待审核
  APPROVED: 1,  // 已批准
  REJECTED: 2   // 已拒绝
}

const STATUS_CONFIG = {
  [REQUEST_STATUS.PENDING]: {
    text: '待审核',
    color: '#faad14',  // 橙色
    icon: 'clock'
  },
  [REQUEST_STATUS.APPROVED]: {
    text: '已批准',
    color: '#52c41a',  // 绿色
    icon: 'check-circle'
  },
  [REQUEST_STATUS.REJECTED]: {
    text: '已拒绝',
    color: '#ff4d4f',  // 红色
    icon: 'close-circle'
  }
}
```

### 错误处理

```typescript
// API 错误处理示例
async function submitDeleteRequest(data) {
  try {
    const response = await api.post('/image-delete/submit', data)
    showSuccessMessage('提交成功，请等待管理员审核')
    return response
  } catch (error) {
    const message = error.response?.data?.message || '提交失败，请稍后重试'
    showErrorMessage(message)
    throw error
  }
}
```

### 图片预览注意事项

1. **图片代理**: 原图URL可能需要通过代理服务器访问（跨域问题）
2. **懒加载**: 列表中的缩略图建议使用懒加载
3. **占位符**: 图片加载失败时显示占位符，特别是已删除的图片
4. **预览组件**: 管理员详情页建议使用图片预览组件支持放大查看

---

## 📝 注意事项

1. **防重复提交**: 用户对同一张图片只能有一个待审核的申请
2. **权限校验**: 用户只能查看自己的申请，管理员可以查看所有
3. **图片可能不存在**: 已批准删除的申请，图片信息字段可能为 null
4. **时间格式**: API 返回的时间为 ISO 8601 格式，前端需格式化显示
5. **分页**: 最大每页50条，前端可根据需要调整默认值

---

## 🔗 相关链接

- 后端实现代码：`f:/yukiryou/setu_api_full/src/main/java/com/example/setuapi/`
- 积分调用页面：(需要在此页面添加申请删除入口)
- 博客调用页面：(需要在此页面添加申请删除入口)
- 管理后台：(需要添加新的管理模块)
